{
  "name": "xroute",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "build": "cargo build -p route-engine && forge build --root contracts/polkadot-hub-router && node --test packages/*/test/*.test.mjs services/status-indexer/test/*.test.mjs",
    "check": "npm run test",
    "test": "npm run test:rust && npm run test:solidity && npm run test:node",
    "test:node": "node --test packages/*/test/*.test.mjs services/status-indexer/test/*.test.mjs",
    "test:rust": "cargo test -p route-engine",
    "test:solidity": "forge test --root contracts/polkadot-hub-router"
  },
  "dependencies": {
    "@polkadot-api/descriptors": "^0.0.1",
    "polkadot-api": "^1.23.3"
  }
}
